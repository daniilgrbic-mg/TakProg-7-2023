#include <bits/stdc++.h>

using namespace std;

int main()
{
    /*
    Tip `int` cuva cele brojeve
    */
    int a; // deklaracija (stvaranje) promeljive bez pocetne vrednosti
    int b = 6; // deklaracije promenljive sa pocetnom vrednoscu

    /*
    Tipovi `float` i `double` cuvaju brojeve sa tackom,
    `double` ima vecu preciznost, pa je cesto bolje koristiti bas double
    */
    float c = 2.5;
    double d = 4.235;

    /*
    Tip `string` sluzi za cuvanje teksta (ili pojedinih reci)
    */
   string skola = "Matematicka Gimnazija";
   string mesec = "oktobar";
}
